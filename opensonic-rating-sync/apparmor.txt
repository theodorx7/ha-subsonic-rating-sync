#include <tunables/global>

profile subsonic-rating-sync flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>

  # Базовые возможности
  file,
  signal (send) set=(kill,term,int,hup,cont),

  # S6-Overlay (если используете base-образ HA)
  /init ix,
  /bin/** ix,
  /usr/bin/** ix,
  /run/{s6,s6-rc*,service}/** ix,
  /package/** ix,
  /command/** ix,
  /etc/services.d/** rwix,
  /etc/cont-init.d/** rwix,
  /etc/cont-finish.d/** rwix,
  /run/{,**} rwk,
  /dev/tty rw,

  # Bashio
  /usr/lib/bashio/** ix,
  /tmp/** rwk,

  # Данные App (options.json, SQLite и т.п.)
  /data/** rw,

  # Python-интерпретатор и пакет
  /usr/bin/python3 rix,
  /usr/bin/python3.* rix,
  /app/** r,
  /app/opensonic-rating-sync/** r,

  # Мапы из config.yaml
  /media/** rw,
  /share/** rw,

  # Сеть для Subsonic API
  network inet stream,
  network inet6 stream,

  # Системные файлы для Python
  /etc/passwd r,
  /etc/hosts r,
  /etc/resolv.conf r,
  /proc/** r,
  /sys/** r,
}
