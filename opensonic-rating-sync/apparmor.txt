#include <tunables/global>

profile subsonic-rating-sync flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>
  #include <abstractions/nameservice>

  # S6-Overlay (базовая инициализация контейнера HA)
  /init ix,
  /bin/** ix,
  /usr/bin/** ix,
  /run/{s6,s6-rc*,service}/** ix,
  /package/** ix,
  /command/** ix,
  /etc/services.d/** rwix,
  /etc/cont-init.d/** rwix,
  /etc/cont-finish.d/** rwix,
  /run/{,**} rwk,
  /dev/tty rw,

  # Bashio (чтение конфигурации и логирование)
  /usr/lib/bashio/** ix,
  /tmp/** rwk,

  # Данные App (чтение /data/options.json, запись SQLite БД и filelock)
  /data/** rw,

  # Python-интерпретатор и его системные библиотеки
  /usr/bin/python3 ix,
  /usr/bin/python3.* ix,
  /usr/lib/python3*/** r,
  /usr/lib/python3*/site-packages/** r,
  /usr/local/lib/python3*/** r,

  # Ваш код приложения (чтение скриптов)
  /app/** rix,

  # Мапы из config.yaml (чтение и запись аудиофайлов и тегов)
  /media/** rw,
  /share/** rw,

  # Сеть для Subsonic API (обращение к Navidrome)
  network inet stream,
  network inet6 stream,

  # Системные файлы для Python (резолв DNS, базовые настройки)
  /etc/passwd r,
  /etc/hosts r,
  /etc/resolv.conf r,
  /proc/** r,
  /sys/** r,
}
